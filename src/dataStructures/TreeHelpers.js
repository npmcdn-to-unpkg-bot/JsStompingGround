
function getTreeHeight(itemCount) {
  return Math.floor(Math.log(itemCount) / Math.log(2));
}
function getCountAtRow(num) {
  return Math.pow(2, num);
}

// given an array, lets visualize it as a binary search tree
export function visualizeTree(a) {
  
}
